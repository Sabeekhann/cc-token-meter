name: Sabee's Bot

on:
  pull_request_target:
    types: [labeled]
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: sabees-bot-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # AI review is opt-in to keep normal pull requests fast and inexpensive.
  # A maintainer starts it by applying the `ai-review` label.
  review-internal:
    if: github.event.label.name == 'ai-review' && github.event.pull_request.draft == false && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Remove any head-provided verdict file
        run: rm -f -- .claude-review-verdict

      - name: Run Sabee's Bot (architect-level Claude review)
        id: claude
        continue-on-error: true
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          claude_args: |
            --model claude-sonnet-5
            --bare
            --tools "Read,Glob,Grep,Write"
            --allowedTools "Write(.claude-review-verdict)"
            --disallowedTools "Bash,Edit,NotebookEdit,mcp__*"
            --max-turns 8
            --max-budget-usd 2
          prompt: |
            You are reviewing this pull request against the base branch, acting
            ONLY as an architect-level reviewer. You are NOT reviewing for
            correctness bugs, security, test coverage, or UX — a human handles
            those. Stay narrowly in scope.

            Review the diff for cc-token-meter, a local-only Node.js CLI + web
            dashboard, against these repo-specific conventions (from
            CONTRIBUTING.md):

            - No framework, no build step. The server uses Node's built-in
              `http` with a tiny manual router; the dashboard is vanilla
              HTML/CSS/JS. Flag any PR introducing a bundler, frontend
              framework, or Express/Fastify-style server.
            - Dependency-light. The repo has exactly two runtime deps (`glob`,
              `open`). Flag any new dependency added without clear, stated
              justification.
            - Pure-function boundary. `src/heuristics/*`, `src/pricing/*`,
              `src/analytics/*`, `src/budget/alerts.js`, and
              `src/ingest/aggregate.js` must stay free of
              `fs`/`http`/network I/O — data in, data out only. Flag any I/O
              creeping into those modules.
            - New heuristics must be one pure function per file in
              `src/heuristics/`, with signature
              `(sessionRecord, toolEvents, allSessionsHistory) => Tip[]`,
              registered in `src/heuristics/index.js`'s `runHeuristics()`, with
              true/false-case fixture tests in `test/heuristics.test.js`.
            - Duplication/reuse: flag logic copy-pasted across modules that
              should instead be a shared, imported function.
            - File placement: product/runtime code belongs in `bin/`, `src/`,
              or `public/`; tests belong in `test/`; documentation belongs in
              `docs/`; repository automation belongs in `.github/`. Flag new
              top-level directories or misplaced runtime code without a
              stated reason.
            - Local-only invariant: flag telemetry, analytics SDKs, remote
              browser assets, non-loopback server binding, transcript writes,
              or any outbound transfer of prompt/tool-result/session data.
            - CI changes must use least-privilege permissions. A
              `pull_request_target` job must never execute fork head code; an
              intentional read-only fork checkout must be isolated behind the
              `external-pr-review` environment.

            Post your findings as normal PR review comments (specific,
            file:line references, no vague generalities). Then, as your final
            action, write EXACTLY one line to a file named
            `.claude-review-verdict` at the repository root:

            - `APPROVE` if you found no violations of the above.
            - `REQUEST_CHANGES: <one-sentence reason>` if you found at least
              one genuine violation.

            Do not write anything else to that file. Do not create the file if
            you are unable to complete the review for any reason.

      - name: Submit PR review verdict
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [ ! -f .claude-review-verdict ]; then
            echo "Sabee's Bot: no verdict produced (review did not complete). Skipping without blocking — a human reviewer should cover this PR."
            exit 0
          fi

          VERDICT_LINE="$(head -n1 .claude-review-verdict)"
          rm -f .claude-review-verdict

          if [ "$VERDICT_LINE" = "APPROVE" ]; then
            gh pr review "$PR_NUMBER" --approve \
              -b "Sabee's Bot (architect-level review): no structural issues found (file placement, duplication, module boundaries, dependency footprint). This is a narrow structural check only — a human reviewer should still cover correctness, security, and tests."
          else
            REASON="${VERDICT_LINE#REQUEST_CHANGES: }"
            gh pr review "$PR_NUMBER" --request-changes \
              -b "Sabee's Bot (architect-level review) found a structural issue: ${REASON}. See inline comments above for detail."
          fi

  # Fork PRs: pull_request_target exposes secrets to a job checking out
  # untrusted fork code, so this job sits behind the "external-pr-review"
  # GitHub Environment, which requires a manual approval click (by a
  # configured reviewer) before anything in the job — including checkout —
  # runs. No fork code executes (no npm ci/install/test/scripts); the Claude
  # step only reads files as text for review, never executes them.
  review-fork:
    if: github.event.label.name == 'ai-review' && github.event.pull_request.draft == false && github.event.pull_request.head.repo.full_name != github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: external-pr-review
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false
          # This job is manually approved through the environment and does
          # not execute repository code. checkout v7 requires an explicit
          # acknowledgement before reading a fork head in pull_request_target.
          allow-unsafe-pr-checkout: true

      - name: Remove any head-provided verdict file
        run: rm -f -- .claude-review-verdict

      - name: Run Sabee's Bot (architect-level Claude review)
        id: claude
        continue-on-error: true
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          claude_args: |
            --model claude-sonnet-5
            --bare
            --tools "Read,Glob,Grep,Write"
            --allowedTools "Write(.claude-review-verdict)"
            --disallowedTools "Bash,Edit,NotebookEdit,mcp__*"
            --max-turns 8
            --max-budget-usd 2
          prompt: |
            You are reviewing this pull request against the base branch, acting
            ONLY as an architect-level reviewer. You are NOT reviewing for
            correctness bugs, security, test coverage, or UX — a human handles
            those. Stay narrowly in scope.

            Review the diff for cc-token-meter, a local-only Node.js CLI + web
            dashboard, against these repo-specific conventions (from
            CONTRIBUTING.md):

            - No framework, no build step. The server uses Node's built-in
              `http` with a tiny manual router; the dashboard is vanilla
              HTML/CSS/JS. Flag any PR introducing a bundler, frontend
              framework, or Express/Fastify-style server.
            - Dependency-light. The repo has exactly two runtime deps (`glob`,
              `open`). Flag any new dependency added without clear, stated
              justification.
            - Pure-function boundary. `src/heuristics/*`, `src/pricing/*`,
              `src/analytics/*`, `src/budget/alerts.js`, and
              `src/ingest/aggregate.js` must stay free of
              `fs`/`http`/network I/O — data in, data out only. Flag any I/O
              creeping into those modules.
            - New heuristics must be one pure function per file in
              `src/heuristics/`, with signature
              `(sessionRecord, toolEvents, allSessionsHistory) => Tip[]`,
              registered in `src/heuristics/index.js`'s `runHeuristics()`, with
              true/false-case fixture tests in `test/heuristics.test.js`.
            - Duplication/reuse: flag logic copy-pasted across modules that
              should instead be a shared, imported function.
            - File placement: product/runtime code belongs in `bin/`, `src/`,
              or `public/`; tests belong in `test/`; documentation belongs in
              `docs/`; repository automation belongs in `.github/`. Flag new
              top-level directories or misplaced runtime code without a
              stated reason.
            - Local-only invariant: flag telemetry, analytics SDKs, remote
              browser assets, non-loopback server binding, transcript writes,
              or any outbound transfer of prompt/tool-result/session data.
            - CI changes must use least-privilege permissions. A
              `pull_request_target` job must never execute fork head code; an
              intentional read-only fork checkout must be isolated behind the
              `external-pr-review` environment.

            Post your findings as normal PR review comments (specific,
            file:line references, no vague generalities). Then, as your final
            action, write EXACTLY one line to a file named
            `.claude-review-verdict` at the repository root:

            - `APPROVE` if you found no violations of the above.
            - `REQUEST_CHANGES: <one-sentence reason>` if you found at least
              one genuine violation.

            Do not write anything else to that file. Do not create the file if
            you are unable to complete the review for any reason.

      - name: Submit PR review verdict
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [ ! -f .claude-review-verdict ]; then
            echo "Sabee's Bot: no verdict produced (review did not complete). Skipping without blocking — a human reviewer should cover this PR."
            exit 0
          fi

          VERDICT_LINE="$(head -n1 .claude-review-verdict)"
          rm -f .claude-review-verdict

          if [ "$VERDICT_LINE" = "APPROVE" ]; then
            gh pr review "$PR_NUMBER" --approve \
              -b "Sabee's Bot (architect-level review): no structural issues found (file placement, duplication, module boundaries, dependency footprint). This is a narrow structural check only — a human reviewer should still cover correctness, security, and tests."
          else
            REASON="${VERDICT_LINE#REQUEST_CHANGES: }"
            gh pr review "$PR_NUMBER" --request-changes \
              -b "Sabee's Bot (architect-level review) found a structural issue: ${REASON}. See inline comments above for detail."
          fi
